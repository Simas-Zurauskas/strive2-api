# Elastic Beanstalk's nginx default client_max_body_size is 1M — far below the
# 50 MB multer cap on course source-document uploads (documentUpload.ts), so we
# raise it to 55M (cap + headroom for multipart framing overhead).
client_max_body_size 55M;
